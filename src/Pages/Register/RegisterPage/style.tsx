import { css } from "@emotion/react";
import { BACK_BASE_COLOR } from "../../../common/style";


export const styles = {
  container:css({
    width: '100vw',
    height: '100%',
    backgroundColor: BACK_BASE_COLOR
  }),
  differenceBox:css({
    marginTop:'10px',
  })
}